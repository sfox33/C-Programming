#include <stdio.h>
#include <stdlib.h>

int main(int argc, const char * argv[])
{
    int firstInteger = 7;
    float firstFloat = 9.0;

    printf("The value of the first integer is %i", firstInteger);
    printf("\nThe value of the first float is %.1f", firstFloat + 0.3);

    return 0;
}

